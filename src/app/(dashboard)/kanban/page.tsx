import KanbanBoard from '@/components/Kanaban';
import React from 'react';

type pageProps = {
    
};

const page:React.FC<pageProps> = () => {
    
    return <div>
        <KanbanBoard/>
    </div>
}
export default page;